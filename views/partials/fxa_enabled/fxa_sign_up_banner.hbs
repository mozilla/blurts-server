    <div id="sign-up-banner" class="sign-up-banner banner {{ class }}">
      <div class="row-wrapper">
        <div class="fourth subscribe-icon-wrapper">
          {{> sprite symID="subscribe"}}
        </div>
        <div class="{{#if featuredBreach.IsSensitive}} three-fourths {{else}} half {{/if}}">
          {{#if featuredBreach.IsSensitive}}
            <h2 class="medium secondary-title">{{getString "get-your-report-and-sign-up"}}</h2>
          {{else}}
            <h2 class="medium secondary-title">{{getString "sign-up-for-fxa-alerts"}}</h2>
          {{/if}}
          <p class="demi">{{getString "create-free-account"}}</p>
        </div>
        {{#unless featuredBreach.IsSensitive}}
          {{> fxa_enabled/sign_up_bundle class="fourth"}}
        {{/unless}}
      </div>
    </div>
