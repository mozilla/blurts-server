{{#getUserPreferences}}
  <section class="container clear-header">
    <div class="row jst-cntr">
      <div class="col-8">
        <h2 class="skinny-headline monitor-preferences-headline">Monitor <span class="bold">{{ getString "preferences" }}</span></h2>
      </div>
    </div>
    <div class="row jst-cntr">
      <div class="col-8 card global-comm">
        <h2 class="section-headline">{{ getString "global-communication" }}</h2>
        <section>
          {{#each communicationOptions}}
            <label class="radio-container">
              <input class="radio-comm-option" data-comm-option="{{ optionId }}" data-form-action=" update-comm-option" type="radio" checked="checked" name="1">
              <span class="radio-label">{{ labelString }}</span>
              <span class="checkmark"></span>
            </label>
          {{/each}}
        </section>
      </div>
    </div>
    <div class="row jst-cntr flx-col">
      <div class="col-8 no-vertical-padding">
        <h2 class="section-headline">{{ getString "email-addresses" }}</h2>
      </div>
          {{#each emails }}
            {{#ifCompare email_addresses.length ">" 0}}
              <div class="col-8 no-vertical-padding">
                <h4 class="email-pref">{{ this.subhead }}</h4>
              </div>
              {{#each this.email_addresses}}
                {{> email-card prefs=./../../../preferences}}
              {{/each}}
            {{/ifCompare}}
          {{/each}}
      </div>
    </div>
    <div class="row jst-cntr">
      <div class="col-8 card bg-dark">
        <h2 class="section-headline">{{ getString "add-new-email" }}</h2>
            {{> forms/add-another-email-form }}
      </div>
    </div>
    <div class="row jst-cntr ">
      <div class="col-8 card">
        <div class="pad-left">
          <h2 class="section-headline">{{ getString "remove-fxm" }}</h2>
          <p class="subhead">{{ getString "remove-fxm-blurb" }}</p>
          <button class="remove-fxm subhead" href="/">{{ getString "remove-fxm" }}</button>
        </div>
      </div>
    </div>
  </section>
{{/getUserPreferences}}
