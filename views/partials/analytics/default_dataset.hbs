data-server-url="{{ SERVER_URL }}"

{{#if FXA_ENABLED}}
data-fxa-address={{getFxaUrl}}
data-utm-campaign="monitor-fxa-integration"
  {{#if req.session.user}}
    data-signed-in-user="true";
  {{else}}
    data-signed-in-user="false";
  {{/if}}
  data-fxa-enabled="fxa-enabled"
{{/if}}
