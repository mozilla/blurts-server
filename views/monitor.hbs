{{!< default }}
<main class="landing-content">
  <section class="section-wrapper">
    <div class="half">
      <h2 data-location="landing-page" class="section-headline">Your right to be safe from hackers starts here.</h2>
      {{#if featuredBreach }}
        <span data-location="featured-breach" class="demi form-desc">Find out if your <span class="bold">{{ featuredBreach.Title }}</span> account was compromised.</span>
        {{> scan_form}}
      {{else}}
        <p class="landing-blurb">
          Firefox Monitor arms you with tools to keep your personal information safe. Find out what hackers already know about you and learn how to stay a step ahead of them.
        </p>
      {{/if}}
    </div>
    <div class="half">
      {{#if featuredBreach}}
        {{> featured_breach }}
      {{else}}
        <span class="demi form-desc">Try it out. Enter your email for a basic scan.</span>
        {{> scan_form}}
      {{/if}}
    </div>
  </section>
</main>

{{> banners/sign_up_banner}}
{{> password_tips }}
{{> banners/download_firefox_banner}}
